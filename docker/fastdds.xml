<!--
  FastDDS Profil für ROS 2 / DLIO
  Zweck: Erhöhung der UDP-Puffergrößen, um große Pakete (z. B. LiDAR-Daten)
         stabil zu übertragen. Dieses Profil wird per
         FASTRTPS_DEFAULT_PROFILES_FILE geladen.
-->
<?xml version="1.0" encoding="UTF-8" ?>
<dds>
  <profiles>
    <transport_descriptors>
      <transport_descriptor>
        <transport_id>udp_transport</transport_id>
        <type>UDPv4</type>
        <maxMessageSize>65536000</maxMessageSize> <!-- 64 MB -->
        <sendBufferSize>65536000</sendBufferSize> <!-- 64 MB -->
        <receiveBufferSize>65536000</receiveBufferSize> <!-- 64 MB -->
      </transport_descriptor>
    </transport_descriptors>

    <participant profile_name="dlio_participant" is_default_profile="true">
      <rtps>
        <builtin>
          <metatrafficUnicastLocatorList>
            <locator>
              <transport_id>udp_transport</transport_id>
            </locator>
          </metatrafficUnicastLocatorList>
        </builtin>
      </rtps>
    </participant>
  </profiles>
</dds>